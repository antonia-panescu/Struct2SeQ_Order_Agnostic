# archive/

Files that are no longer the canonical version of their thing but are
kept for provenance. Treat everything here as **read-only history**;
the current entry points live at the repo root or under `evaluation/`.

See `docs/history/deprecated_approaches.md` for the why behind each
archived file.
